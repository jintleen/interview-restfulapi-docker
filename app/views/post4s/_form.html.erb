<%= form_for(@post4) do |f| %>
  <% if @post4.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post4.errors.count, "error") %> prohibited this post4 from being saved:</h2>

      <ul>
      <% @post4.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
